# $fbsd_header

PORTNAME=$name
PORTVERSION=$version
CATEGORIES=$fbsd_categories
MASTER_SITES=$fbsd_sites

MAINTAINER=$contact
COMMENT=$descr
USES=gmake
LIB_DEPENDS=libpcap.so:net/libpcap

LICENSE=GPLv3

WRKSRC=${WRKDIR}/${PORTNAME}-${PORTNAME}-${PORTVERSION}

.include <bsd.port.mk>
